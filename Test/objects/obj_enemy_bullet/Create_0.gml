/// @description 
direction = 0;
image_angle = point_direction(x, y, obj_player.x, obj_player.y);
_direction = point_direction(x, y, obj_player.x, obj_player.y);
beforeX = x;
beforeY = y;

alarm[0] = 100;

